{
  "name": "pharmacy-erp-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Browser smoke tests and seeded workflow probes for the pharmacy ERP.",
  "scripts": {
    "seed": "node scripts/seed-smoke-data.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "list": "playwright test --list",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  },
  "dependencies": {
    "pharma-management-system-monorepo": "file:.."
  }
}
